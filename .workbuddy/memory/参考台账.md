# 参考台账 —— daily-learning

MEMORY.md 的查表附表。只在需要具体数字/清单时读，不随每轮注入。

## A. 试卷结构数字
- 高数一：新结构(2024+) 选择 12×7 + 填空 3×7 + 解答 3×15 = 18 题；旧结构(2015-2023) 选择 10×4 + 填空 10×4 + 解答 8 题（8×5+10×3）= 28 题 150 分。
- 政治：选择 35×2 + 简答 4×10 + 论述 2×20 = 150 分；配额 10 哲学 / 10 毛中特 / 10 习概 / 5 时事。
- 英语：语音 5×1 + 词汇语法 15×1 + 完形 15×2 + 阅读 20×3 + 对话 5×3 + 写作 1×25 = 150 分。
- 高数一题型按题号位置：旧结构 so0-9 选择 / 10-17 解答 / 18-27 填空；新结构 so0-11 / 12-14 / 15-17。

## B. 英语补全对话
- 功能判定锁死规则：① Sure / Of course → 邀请请求 ② 给时间或数字 → When / How long ③ Thank you → 帮忙给东西 ④ You too → 祝福 ⑤ 最后一空 = 告别。
- 答案序（56→60）：
  2014 E-C-B-G-H／2015 B-D-H-A-E／2016 H-D-G-E-F／2017 A-F-E-H-C／2018 H-E-G-A-C／2019 D-B-H-A-E／2020 D-F-B-A-C／2021 E-F-C-H-B／2022 A-C-H-E-D／2023 E-A-C-H-D／2024 H-C-B-G-E／2025 D-G-?-B-F
- 已知数据缺陷：2018 options 缺 H；2025 第 58 空应为 E。

## C. 考点增补内置条目（共 20 条，两批）
- 批次一 `2026-09-22`（15 条）：习概 7（经济 / 政治 / 文化 / 社会 / 生态 / 党建 / 外交，含 2025-12 中央经济工作会议、二十届四中全会与十五五、抗战胜利 80 周年、生态环境法典、全球治理倡议）+ 时政 4（主场外交 / 重大工程科技 / 经济数据首次突破 / 国家表彰命名）+ 哲学 3 动作卡 + 毛中特 1 状态卡。
- 批次二 `2026-09-23`（5 条）：习概 3（2026 预期目标与十五五主要指标 / 设立台湾光复纪念日 / 2026 上半年重要指示与文件）+ 时政 2（国防现代化里程碑——福建舰入列 / 重大体育赛事——十五运会·米兰冬奥）。
- 全部 `status=done`（首灌状态），学习态 `mastery` 一律 0；均带出处（新华社 + 日期）。

## D. 四桶元数据（前端常量，不入库）
- 位置：`packages/web/src/utils/tradeoffPlan.ts`。
- 四桶色值：习概 `--wb-warn`、时政 `--wb-brand`、哲学 `--wb-accent`、毛中特 `--wb-faint`。
- 内容：每年损失 / 可回收 / 折合分 / 时间窗 / `ENTRY_RULES` / 考场规则（EXAM_RULES）/ 被否技巧（DEBUNKED）/ 25 天日程（SCHEDULE）。改数字前先复算命中率。

## E. 必背考点正文分层类名
- `decorateProse()` 注入的 `.p-*` 语义类：`p-h` 标题、`p-sec` 小节、`p-box` 普通块、`p-formula` 公式、`p-stats` 数据、`p-rows` 键值行、`p-table` 表格、`p-list` 列表、`p-p` 段落。
- 承接容器：PC `.mr-rich`，H5 `.mmr-rich`。
- 判定阈值：同类项 ≥2 且占比 ≥70%；否则退化为 `p-box`。
- 离线回归脚本：`/tmp/prose_check.py`（Python 复刻判定逻辑，对 35 条 MATERIAL 全量跑）。

## G. 输入框聚焦样式全表（2026-09-23 统一后）
**规矩：只改容器边框色，禁止 `box-shadow` 光圈与 `outline`。**

| # | 选择器 | 触发 | 聚焦表现 | 位置 |
|---|--------|------|----------|------|
| 1 | `.wb-input` / `.wb-select` / `.wb-textarea` | `:focus` | 边框 `--wb-brand`（#28b894） | design-system.css:178 |
| 2 | `.wb-search`（容器） | `:focus-within` | 边框 `--wb-brand` + 内部图标转 `--wb-brand` | design-system.css:195 |
| 3 | `.mr-search`（必背页搜索，复用 .wb-search） | `:focus-within` | 同 #2，图标 `--wb-brand` | MustReadView.vue:636 |
| 4 | `.wb-search`（真题页局部副本） | `:focus-within` | 同 #2 | PapersView.vue:1638 |
| 5 | `.availability-grid label > div:not(.time-range)` | `:focus-within` | 下边线 `--wb-brand` | PlanSettingsView.vue:198 |
| 6 | `.time-range` | `:focus-within` | 下边线 `--wb-brand` | PlanSettingsView.vue:206 |
| 7 | `.m-shell input/select/textarea` | `:focus` | 边框 `--app-primary`（#2563eb） | main.css:91 |
| 8 | `.search-form label`（移动端全局搜索） | `:focus-within` | 边框 `--app-primary` | MobileSearchView.vue:139 |
| 9 | `.study-page .van-search__content` | `:focus-within` | 边框 `--app-primary` | main.css:239 |
| 10 | `.study-page .van-cell`（van-field 表单列） | `:focus-within` | 下边线（`::after`）`--app-primary` | main.css:232 |

- **保留不动**：按钮 / 链接 / 卡片 / 页签的键盘 `:focus-visible` 3px 轮廓（`.wb-btn`、`.wb-seg-item`、`.topbar-toggle`、`.wb-card`、`.q-option`、`.q-self-btn`、`.mr-toc-item`、`.mr-refs-toggle`、`.next-task`）—— 与「输入框不加光圈」是两件事，属可访问性。
- `main.css:88` 的全局轮廓规则已从 `button, a, input, select, textarea` 收窄为 `button, a`。
- 历史沿革：改前共五套（`.wb-input` 绿框+绿光晕、`.wb-search` 紫框+紫光晕、全局 `input:focus-visible` 蓝轮廓、`MobileSearchView` 写死 `#7fabfb`+光晕、vant 搜索框零反馈只有浏览器默认轮廓）。统一于 2026-09-23。

## F. 验收脚本清单
- `verify_must_read` 68 项 + `math_patch` 45 项 + `mr_verify` 43 项 + 真题页回归 87 项。
- `verify_tradeoff2.py`（考点增补：掌握度指标、滚动保持、不熟清单口径、空态）。
- pnpm 前置：`corepack enable --install-directory ~/.workbuddy/binaries/node/versions/22.22.2-3/bin`。

## H. outputs/ 存量清单（2026-09-23 清理后）
- 目录现状：**8 份 HTML + `assets/`，共 4.3 MB**（清理前 57 个文件 / 24 MB）；PNG 已全部清空。
- **保留 8 份**（均无 App 页面承载，删了会丢内容）：
  - `知识点清单-政治/英语/高等数学一.html` —— 161 条 L1–L4 难度分级清单 + 学习路线；**未入库**（条目名与库内标题重合仅 10%）。
  - `必备题集-政治/英语/高等数学一.html` —— 各 30 题，同样按 L1–L4 分级。
  - `政治-主观题命题规律分析.html`、`英语-发音题满分攻略.html`。
- **已删 34 份**，两类：①「已入库」＝三份 5周冲刺保底方案（→ `builtin-must-read.ts` 280 条）、必背考点模块-迁移工作量审计、高等数学一-2020真题核对；②「分析/验证已完结」＝三科考点分析报告、政治选择题-逐年命中实测、题库覆盖总览-2014至2025、试卷结构核查 2 份（2026题型结构变动 / 8题解答题疑点）、英语-2024阅读定位法审计、移动端页面-真机预览、政治-未命中题取舍方案。另有 7 张旧版面截图 + 14 张 UI 验收截图（全部 PNG）。
- **判定方法（可复用）**：不要凭文件名猜「是否入库」。用 python 把 HTML 内的条目标题与 server 侧 `builtin-*.ts`（must_read 280 / knowledge 74+41 / tradeoff 20）做**标题级交集**：重合率高＝已入库；低＝独立内容（知识点清单仅 10%）。
- `assets/`：`mathjax/tex-svg-full.js`（2.2 MB）仍被 4 份报告依赖 → **不可删**；`katex/`（1.4 MB）已零引用（唯一使用者「移动端页面-真机预览.html」已删）→ 可删。
- ⚠️ **归档核对教训**：macOS `bsdtar` 打包会额外写入 `._` 开头的 AppleDouble 元数据条目，且 `tar -tzf | grep -qx` 在本环境会**误报文件缺失**。核对归档一律用 `python tarfile` 读成员名 + size 与磁盘逐一比对。
- ⚠️ **git 陷阱**：outputs/ 全部文件处于「已 add 未 commit」状态，删除工作区文件后 `git status` 会出现 `AD` 条目 —— 此时若直接 commit，被删文件会从索引「复活」提交。删完必须 `git add -A outputs` 同步索引。
- 备份路径（若仍在）：`~/Desktop/study/outputs-backup-2026-09-23.tar.gz`，2026-09-23 已按用户指令 `mv` 进 `~/.Trash/`，保留全部 58 个原始条目。

## I. 文档体系与缺口（2026-09-23 排查）

现存 7 份 md，**全部已入库且全部停在 2026-08-10/11**：

| 文件 | 大小 | 定位 |
| --- | --- | --- |
| `README.md` | 1.6K | 仓库概览、技术栈、本地启动、体验账号 |
| `docs/README.md` | 12.8K | **唯一详细说明**：产品范围／功能表／架构／PC+H5 路由／API 模块／数据表／真题规则／开发部署／维护约定／待补项 |
| `docs/DEPLOYMENT.md` | 12.8K | 生产部署全流程 8 节；Docker 三容器，Web 本地 build 后上传 |
| `packages/server/README.md` | 3.9K | 服务端索引：快速定位／模块表／内置数据／表／命令／环境变量 |
| `packages/web/README.md` | 4.6K | Web 索引：快速定位／路由表／分层／Store／UI 维护规则 |
| `docs/plans/2026-08-11-vocabulary-rapid-mode.md` | 19.6K | 单次设计方案（词单快速模式） |
| `docs/releases/2026-08-11-*.md` ×3 | 5.8/8.3/10.6K | 发布说明，含 migration 顺序与回滚 |

- **无接口文档，且是刻意的**：未装 `@nestjs/swagger`、无 `SwaggerModule`；`docs/README.md` §5 末句写明「具体请求字段以各模块 `*.controller.ts` 与 `*.dto.ts` 为准，避免维护容易失真的手写接口副本」。
- 缺口（08-11 之后的功能一律未进文档）：
  - **PC 路由** 7 → 11，缺 `/papers`、`/papers/:paperId`、`/must-read`、`/tradeoff`
  - **H5 路由** 9 → 13，缺 `/m/must-read`、`/m/mistakes`、`/m/papers`、`/m/papers/:paperId`；`meta.hideTabbar` 未记
  - **API 前缀** 9 → 10，缺 `/tradeoff`（auth / subjects / plan / knowledge / papers / mistakes / vocabulary / essays / sessions / tradeoff）
  - **数据表** 两份 README 清单均缺 `tradeoff_items`；且**未写明「必背考点不是独立表 —— 存在 `knowledge_items`，靠 `extra.origin='sprint5w'` 区分，经 `/knowledge` 提供」**（最易误判处）
  - **功能表** `docs/README.md` §2 八行缺 必背考点 / 考点增补 / PC 真题页与答题模式 / H5 错题本
  - **种子与命令** 缺 `builtin-must-read.ts`、`builtin-tradeoff.ts`、`import-must-read.ts`、`import-tradeoff.ts`，以及 `pnpm db:must-read`、`pnpm db:tradeoff`
- **过期数字与其暴露的问题**：`docs/README.md` §7 仍写「8 套、373 题」（2023 英语 61 题／2025 英语 53/61／2024 高数一 9/18），而 `builtin-verified-papers.ts` 现在只剩 2023 —— **2014–2025 真题只存在于数据库，仓库内没有可重建的工具或种子**（无 `scripts/`、无 tools 目录），文档亦未说明这一事实。
- **两处不一致**：① `docs/README.md` §9-2 与 `packages/web/README.md` UI-4 均写「不新增／不保存独立 HTML UI 原型」，但 `design-demos/vocabulary-rapid-mode/`（index.html + 2 png）仍在；② CI `.github/workflows/ci.yml` 只跑 `pnpm lint` + `pnpm build`，**不校验文档**，故文档腐烂不会被流水线发现。
- 实际接口面（核对用）：**10 个 controller、65 个端点**（auth 3 / essays 5 / knowledge 5 / mistakes 6 / papers 7 / plan 8 / sessions 3 / subjects 4 / tradeoff 6 / vocabulary 18）；`/knowledge` 同时承载知识库与必背考点，**无独立 must-read controller**。
