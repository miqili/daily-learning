# 2026-09-13 工作日志

## 英语保底方案增补：音标 / 中文 / 点读发音

用户反馈：英语方案里"有很多英语不认识"，要求**需要记的内容加音标 + 发音 + 中文**。

### 改动文件
`outputs/英语-5周冲刺保底方案.html`（原 30010 字节 → 52876 字节）
备份：`/tmp/英语-5周冲刺保底方案.bak.html`

### 具体做法
1. **单词级**：全站 192 处英文单词/词组加 `.spk` 可点击朗读 + `.ipa` 英式音标 + `.cn` 中文。
   - 第四节「同义替换套路」由纯文本 box 改为 11 行表格：原文词 / 替换词 / 中文，全部带音标。
   - 第七节「语音辨析」表格加音标与中文，**例外词标 ▶例外**，48 个例词全部可点读。
   - 第八节「固定搭配」12 条加音标；虚拟语气信号词 5 个加音标。
2. **句子级**：第五节 12 类对话句型，每类拆成 **英文行（可点读）+ 中文行**；共 23 处 `.spk-line`。
3. **范文级**：第六节 4 套作文模板各加 **🔊朗读这篇范文按钮**（`data-read="#tplN"`，读元素 innerText，不重复维护文本）
   + **参考译文块**（`.cntr`），共 4 个。
4. **新增第十二节「必背词汇总表（音标·中文·点读）」**：81 条 `.v` 条目，分 3 组
   （① 语音辨析必背词 48 条 ② 阅读同义替换词 19 条 ③ 固定搭配 14 条），双栏 `column-count:2` 布局。
   原「一句话行动清单」顺延为第十三节。
5. **发音实现**：页尾内联 `<script>`，`speechSynthesis` + Web Speech API，优先 en-GB 音色，`rate=0.9`，
   **完全离线可用**（不依赖外部音频文件）。事件委托监听 `[data-say]`（单词/句子）与 `[data-read]`（范文整篇）。
6. 页首加 `.box.audio` 使用说明（告诉用户"带下划线的词都能点"）。

### 验证（全部通过）
- 完整性：旧文件 473 个英文词 **0 丢失**；32 项关键数字/字符串全部在位（125/60/75%、37.8%、9.67 题/年、全部时间表等）。
- 渲染：Playwright + 系统 Chrome → 页面错误 0、失败请求 0、横向溢出 0px；系统语音库 191 个。
- 交互：模拟点击 `.spk` / `.spk-line` / `[data-read]` 三种入口无异常。

### 复用脚本
- `/tmp/verify_eng_new.py` —— 新旧文件完整性 + 增强层计数（可改路径复用于其它科目）
- `/tmp/verify_eng_render.py` —— 渲染 + 点读交互 + 布局溢出检测

---

## 英语补全对话：改写为「零基础形状法」（用户第二轮反馈）

用户反馈三点：①补全对话**不知道对话意思时非常容易做错**；②**12 类功能句型对其无用**（自称"英语基本 0 基础"）；③**做的正是 2021 年那套**。

### 关键发现：库里没有对话正文
`exam_questions` 只存了 5 个问题（"选择最佳表达填入对话第 56 空"）+ 8 个选项 A-H + 答案，**对话正文不在库里**。这正是原方法失效的根因——没正文，"背句型"无从落地。
→ 解决办法：**从 12 份 PDF 原卷逐份抽取对话正文**，与库内选项/答案对齐。

### 提取与验证（脚本）
- `/tmp/dlg_extract.py` + `/tmp/dlg_extract2.py` + `/tmp/dlg_old.py` —— 12 年对话正文抽取（2014-2017 需按人名模式兜底，2018+ 按选项指纹定位）
- `/tmp/dlg_all.json` —— 12 年对话正文（2015/2019/2020/2022/2024 有跨页或前缀污染，需清洗）
- `/tmp/dlg_final.py` → `/tmp/dlg_final.json` —— 每年答案映射 + 多余项
- `/tmp/dlg_shape.py` —— 形状法量化验证
- `/tmp/sec5_new.html` —— 新第五节内容
- `/tmp/splice.py` + `/tmp/fix_app.py` + `/tmp/patch3.py` —— 替换第五节 + 生成附录

### 新方法：形状法（核心结论）
补全对话 = 一问一答的链条，**空隔壁那句的"形状"就决定了答案类型**。零基础只需认 3 种形状：数字、大写字母、几个高频小词。
- **形状对应表**（9 行）：下一句是时间/数字→问 When；是地名/名称→问 Where；是 Yes/Sure→前面是疑问句；是 Thank you→前面是"帮了忙"；是 You too→前面是祝福；对话最后一空→告别
- **四条锁死规则**：①Sure/Of course/Yes please→前面是邀请或请求 ②给出时间数字→前面问时间/数量 ③Thank you→前面是帮了忙/给了东西 ④You too→前面是祝福
- 期望值：盲填≈2 分；背 4 条锁死规则→6-9 分；加形状表+排除法→**9-12 分**

### 2021 年真题（用户做的那套）· 零基础复盘
```
Daniel: How are you doing, Linda?
Linda : To be honest, I am really tired of my work at the moment. (56)
Daniel: My friends and I are planning a trip on Sunday.          (57)
Linda : Sure, I'd love to.                                       (58)
Daniel: The Golden Beach. We will have a picnic there.
Linda : I can't wait!                                            (59)
Daniel: Eight o'clock in the morning. We'll pick you up.
Linda : Great!                                                   (60)
```
答案 **56=E 57=F 58=C 59=H 60=B**（多余项 A/D/G）。零基础可 5/5：57 靠 "Sure, I'd love to"→邀请；58 靠 "The Golden Beach" 大写→Where；59 靠 "Eight o'clock" 数字→When；60 靠"最后一空=告别"；56 靠 tired/work/break。

### 交付结构（英语方案现 14 节）
第五节整节替换 → 5.1 原理 / 5.2 形状对应表 / 5.3 四条锁死规则 / 5.4 **2021 完整复盘** / 5.5 考场 3 步 / 5.6 钥匙词卡（35 词，音标+点读）/ 5.7 期望值表
**新增第十四节**：附录·补全对话 12 年真题原文与答案（练习用，60 个空号高亮 + 答案 + 多余项）

### ⚠️ 发现 2 处题库数据问题（已写入报告，未擅动库）
1. **2018 年选项缺失 H**：`options_json` 只录到 A-G，按 PDF 原文 H 应为 `where to`（即第 56 空答案）。
2. **2025 年第 57、58 空答案重复记为 G**：与"8 选 5 不重复"矛盾，多余项变成 4 个。按对话逻辑 **58 应为 E "I don't think the test was hard."**（Ryan 说"没那么紧张"→"我觉得题不难"→Nick 接"大部分题挺简单"）。报告已双轨标注。

### 最终验证
- 关键数据 32 项全部在位；旧第五节删除属预期（10 个词如 cheer/mind/matter 已补回钥匙词卡）
- 渲染：页面错误 0、失败请求 0、无横向溢出；点读元素 232 个、音标 229 条
- 14 节编号连续；附录 12 年 × 5 空 = 60 个空号全部标记正确

---

## 政治保底方案增补：必背清单挂接关联真题（默认折叠）

用户需求：`政治-5周冲刺保底方案.html` 的**必背清单把每条关联的真题题型显示出来，默认隐藏**。

### 改动文件
`outputs/政治-5周冲刺保底方案.html`（26602 → 94870 字符 / 磁盘 132K）
备份：`/tmp/政治-5周冲刺保底方案.bak.html`

### 做法：相似度自动匹配 + 人工校正
1. **取条目数据**：用 `ast` 解析 `/tmp/gen_pol_sprint.py` 里的 `MAO`/`XJP`/`PHIL` 三个列表（96 条，格式 `(问题, 答案, 年份串)`），**不 import**（该脚本会执行生成逻辑）。
2. **匹配算法**：字符 bigram 重合率
   `sim = 0.55*sim(条目标题, 真题题干) + 0.45*sim(条目答案, 正确选项文本)`，
   在条目 `.yr` 标注的年份内取最高分题。全库 526 题。
   → 96 条中 **91 条自动匹配 ≥0.65**，5 条偏低需人工。
3. **人工校正（选择题 overrides，按条目序号）**
   | 条目 | 误配 | 正确 |
   |---|---|---|
   | 73 划分唯物主义和唯心主义的标准 | 2024 第26题(习概,人才) sim0.15 | **2024 第1题**（对存在和思维何者为第一性…） |
   | 74 物质的唯一特性 | 2021 第7题(检验真理标准) | **2021 第3题**（列宁的物质定义…客观实在性是） |
   | 34 社会主义核心价值观 | 2023 第20题(两步走) | **2023 第15题**（富强民主文明和谐=国家层面） |
   | 95 人民群众是历史的创造者 | 2021 第24题(法治保证) | **2021 第9题**（党的群众观点和群众路线的理论依据） |
4. **主观题挂接（`OVR_SUB`，按同一考点人工精选 22 条条目 / 30 条记录）**
   主观题（简答+论述）用纯相似度噪声极大（会把无关题拉进来），**必须人工甄别**。
   高置信例子：科学发展观←2023简答38；感性认识与理性认识←2023论述40；全过程人民民主←2024论述41；
   总体国家安全观(政治安全)←2025简答39；社会存在与社会意识←2023简答36；量和质变(铁杵磨针)←2021论述40；
   党的领导(法治最根本保证)←2021简答39 + 2022论述41 + 2023简答39；人民群众←2022论述40 + 2025简答36。
   ⚠️ 曾误挂 条目33「转变经济发展方式的出发点和落脚点」→2022简答39(邓小平市场经济)，已删除。

### 前端实现
- 每条目包一层 `.kpi`，后跟原生 `<details class="kpd"><summary>查看关联真题（N 题）</summary>`，
  **原生 details 即默认折叠，无需 JS**，离线可用。
- 每条真题渲染：题型 pill（单项选择题=p-acc / 简答题·论述题=p-amb）+ 年份·题号 + 分值 + 题干（截 240 字）
  + 4 个选项（正确项 `.kpa` 绿色 + ✓）；主观题无选项则显示参考答案（截 100 字）。
- 每组 `.card` 顶部加 `「展开本组全部真题」` 按钮，JS 按 `closest('.card')` 作用域批量开关（3 个按钮，各控一组）。
- 新增 CSS `.kpi/.kptools/.kpbtn/.kpd/.kpbody/.kpq/.kphead/.kpstem/.kpopt/.kpa`；注意**不要用 `.ok`**
  （已被 `.box.ok` 占用 background/border-left），故正确项类名用 `.kpa`。

### 覆盖与验证
- **96 条全部挂上**（1 题 58 条 / 2 题 26 / 3 题 4 / 4 题 7 / 5 题 1）；共 155 条真题记录 = 124 选择题 + 31 主观题
- **选择题无重复引用**（一一对应）；主观题允许同一题挂多条条目（同一考点）
- Playwright 实测：details 96 个、**默认展开 0**；点单条 → 展开 1；点组按钮 → 该组 36 条全开；
  **页面错误 0 / 失败请求 0 / 横向溢出 0px**
- 原文件关键内容零丢失（h2 11 个、table 6 张、qno 96 个、yr 标签 96 个均不变）

### 复用脚本
- `/tmp/match_kp.py` + `/tmp/final_match.py` —— 选择题自动匹配 + 可疑项标记
- `/tmp/inspect_low.py` —— 低分条目候选（top5）导出，供人工校正
- `/tmp/match_sub.py` —— 主观题候选
- `/tmp/enrich_kp.py` —— **最终生成器**：含 overrides 表、details 渲染、CSS/JS 注入
- `/tmp/verify_pol_kp.py` —— 折叠交互 + 渲染自检
- 注意：**重跑前必须先 `cp /tmp/政治-5周冲刺保底方案.bak.html` 回滚**，否则会二次插入

---

## 政治必背清单覆盖率复审（用户问："11-30 题是不是背清单就够 / 每年是不是都围绕清单"）

### 结论
2021-2025 五年、11-30 题共 100 道 → 剔除 5 道串位题（2023 年 4 道时政 + 1 道哲学串位），
有效 95 题，其中 **90 题能在 72 条毛中特+习概清单里找到对应条目 = 94.7%**。
分年：2021 89% / 2022 95% / 2023 94% / 2024 100% / 2025 95%。

### ⚠️ 发现 5 个清单缺口（尚未补录，待用户确认）
| 考点 | 真题 | 答案 |
|---|---|---|
| 改革发展稳定三者关系 | 2014#22 / 2015#24 / 2017#25 / 2019#23 / 2021#25 选择 + 2016#43 / 2018#45 简答（史上 7 次） | 改革=动力、发展=目的、稳定=前提；结合点=改善人民生活 |
| 遵义会议 | 2022 第 12 题 | 开始确立毛泽东正确路线在党中央领导地位的会议 |
| 党的基层组织 | 2025 第 18 题 | 党的全部工作和战斗力的基础 |
| 三个务必 | 2023 第 27 题 | 敢于斗争、善于斗争 |
| 四个伟大 | 2021 第 26 题 | 起决定性作用的是伟大工程 |

### 关键认知（重要）
- **题号分块只在 2021 年后的新结构成立**：1-10 哲学 / 11-20 毛中特 / 21-30 习概 / 31-35 时政。
  旧卷（2014-2020，40 题选择）分块完全不同——2018 年是 1-10 时政 + 11-20 哲学 + 21-40 毛中特，
  **不能按 11-30 定位**；统计时必须先筛年份。
- **库内 sort_order 有串位**：2021 第 10 题实为毛中特、第 16 题实为哲学；2023 第 6-10 题全是毛中特。
  按题号算覆盖率会被污染，须按内容判断。
- 哲学题（1-10）对 PHIL 清单吻合度明显低于 11-30（含串位题按题号算仅 58%），
  因哲学偏理解 + 爱出新材料题（2025#2"人工智能是否属物质范畴"、2025#9"普遍交往"、2024#2"人工智能是意识能动性的特殊表现"）。

### 复用脚本
`/tmp/cov4.py`（全局池匹配）→ `/tmp/cov_new2.json` → `/tmp/final_stat.py`（分类统计）
`/tmp/cov_phil2.py`（哲学覆盖率）、`/tmp/gap_detail.py`（缺口真题详情）
