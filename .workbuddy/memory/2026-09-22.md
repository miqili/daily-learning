# 2026-09-22

## 移动端页面「照搬 PC」返工（用户明确指出，10:45）
- 背景：9-21 新建的 `/m/must-read`、`/m/mistakes` 虽是独立页面，但**内容与样式沿用了 PC 写法**，用户要求遵循移动端 UI 规范并重做。
- 照搬点（已全部修掉）：
  1. **复用 PC 组件** `MustReadRefs`（用 `--wb-*`/`wb-btn`/`wb-tag`、12px 文字链接、靠 `:deep` 放大字号）→ 新建 `components/mobile/MobileMustReadRefs.vue`（折叠头 48px、题目卡、44px 通栏跳转按钮、选项整行高亮、370px 窄屏断点）。
  2. **数据表用原生 `<table>` + 横滚**（手机上行首会滚出视野）→ 改成「首列作标题 + 次列副标题 + 其余列键值块」卡片；`tableCells()` 会跳过与 rowMeta.tag 文案重复的末列（如「必背 · 4 题」）。
  3. **整卡 `overflow-x:auto`** 导致公式溢出时标题文字一起滚 → 只让 `.mmr-formula` / `.katex-display` 自身滚动。
  4. **自造 `.mm-filter`(32px) / `.mm-btn`(42px)** 脱离移动端既有视觉 → 改用 main.css 全局 `.study-filter`(44px pill) / `.study-primary` / `.study-secondary`。
  5. 裸 hex（`#f59e0b`、`#ecfdf5`、`#047857`）→ 改用 `--app-warning/--app-success/--app-danger`。
  6. 两个页面重复写 `min-height: calc(100vh - 64px)`（main.css 的 `.study-page` 已有，且 `.m-main` 已含 `padding-bottom: calc(72px + safe-area)`）→ 删掉，只留主题色。
  7. 删除按钮 28px → `van-swipe-cell` 右滑删除（vant 4.10，`#right` slot；`.van-swipe-cell__right` 自带 `height:100%`，按钮 `height:100%` 生效）。
- 答题页顺带修：主观题自评按钮 34px→40px、错因 chip 32px→40px、`.p-pop-foot` 补 `env(safe-area-inset-bottom)`。
- 移动端规范基线（新增页面必须遵守）：容器/分段/筛选/按钮一律 `.study-*`（44px 触控）；正文 14–14.5px、辅助 11.5–12.5px；`-webkit-tap-highlight-color:transparent` + `:active` 反馈；只让公式局部横滚；补 `@media (max-width:370px)`；颜色只用 `--app-*`。
- 产出：outputs/移动端页面-真机预览.html（390×844 三屏静态预览，真实数据 + 本地 KaTeX，assets/katex 已从 node_modules 复制）。构建 `vite build --emptyOutDir false` 通过。

## 必背考点排版分层改造（下午追加）
- 用户反馈：章节标题「一、」太丑、正文密密麻麻层次不分明。
- 新增 `packages/web/src/utils/prose.ts`：`decorateProse()` 渲染前给裸 HTML 打 `.p-*` 语义类（p-h/p-sec/p-box/p-formula/p-stats/p-rows/p-table/p-list/p-p），判定保守（≥2 项且占比≥70%），猜不中退化为 p-box。Python 复刻校验脚本 /tmp/prose_check.py 对 35 条 MATERIAL 全量回归。
- MustReadView.vue：章节头重做（32px 实心科目色序号徽标 `.mr-chapter-no`、目录小徽标、meta 药丸），正文样式迁到 `.mr-rich`；MobileMustReadView.vue 同步（`.mmr-rich`，表格只自身横滚）。
- 关键 bug：`el.querySelector('div div')` 在 el 自身是 div 时会把子 div 全部命中 → 指标卡判定永远失败；改为 `kids.some(k => k.querySelector('div'))`。
- 验证：vite build 通过；Playwright 实测 p-stats/p-rows 均 grid、徽标 32px 科目色；截图 5 张存 outputs/。
- 联调坑新增：server 必须从仓库根启动否则 JWT secret 走默认值全站 401；Playwright 注入 localStorage 会被 store restore 清空 → 用 route 拦截补 Authorization（route.fetch 参数是 post_data）；chromium 需显式 executable_path。
- MEMORY.md 已精简重写（12.9k→约 7k 字符）。

## PC 误改回退（下午追加 2）
- 用户反馈：上一轮只让改 H5，AI 连 PC MustReadView.vue 一起改了 → 全量回退 PC。
- 关键教训：对同一文件**并行发多个 Edit 会互相覆盖**（部分 Edit 显示 Success 但未落盘）——同文件多处修改要么串行 Edit，要么一次 Python 脚本完成；且 Edit/Read 与 Bash 视图可能不同步，以 Bash grep 为准验收。
- 回退方法：PC 端 prose/decorateProse、mr-rich、章节头徽标、--ch-tone 全部还原（保留更早会话的 KatexRenderer title/note、三角表考频色标）；utils/prose.ts 保留（H5 在用）。
- H5 验收：三科 41 个正文章节 DOM 统计，p-sec/p-table/p-stats/p-rows/p-formula 命中正常；截图 5 张存 outputs/必背考点-H5*.png；失效 PC 截图移至 /tmp/old_pc_shots。

## 英语语音题（第1-5题）考点归因
- 数据：库内 2014-2025 英语真题 60 道语音题（exam_papers.subject='英语' + sort_order<5；注意 exam_papers **无 subject_id 字段，用 subject 字符串**）。
- 答案字段格式 `【答案】X\n\n【解析】…`，必须只取【答案】后第一个字母，否则解析文本里的字母会污染（2023-2025 曾取成 DABCD…）。
- 画线标记：2018-2022 用 `[xx]`，2023-2025 用 `<u>xx</u>`，2014-2017 无标记需推断。
- 归因数据脚本 /tmp/pron_data.py（60 题 × 画线/族/odd词/发音），报告 outputs/英语-发音题满分攻略.html。
- 结论：16 个考点族。s清浊7、哑音字母7、ow/ou/oo 6、tion/sion/ture 5、a的读法5、ea 4 → 前6族覆盖57%；th/u/ear-our 各3。答案分布 A14/B17/C15/D14 均匀，无位置技巧。唯一 odd 词 58 个（flood、fear 各考2次）。

## 政治必背清单排序答疑 + 发现年份徽标不一致（22:35）
- 用户问「为什么不是按出题顺序排」。核查链路：outputs/政治-5周冲刺保底方案.html → builtin-must-read.ts → knowledge_items.sort_order（origin=sprint5w）→ MustReadView 前端按返回序分章/分组/组内渲染（后端只有在 sortOrder 为 NULL 时才排后，无二级重排）。
- 结论：**两层排序都是刻意的**。① 章节层＝按投入产出比（源文档第三章标题即为「按投入产出比排序」）：四毛中特20分(背诵型) → 五习概20分 → 六哲学20分(理解型，只抓高频) → 七时政10分(考前7天)，与卷面题号区间（1-10哲学/11-20毛中特/21-30习概/31-35时政）刻意相反；② 组内层＝知识点逻辑链（教材/历史叙述顺序，如「形成与灵魂」组：命题提出→七大确立→精髓→萌芽标志→初步形成标志→第一次飞跃），非年份/题号序——因为同一考点跨年重复考（如社会主义核心价值观合并 2023#15+2022#25），按年会拆散逻辑链。
- 按题号/年份定位的需求由卡片「出处」灰字（年·题·正确项原文）+「查看关联真题」折叠面板承担；第二章另有板块配额表给出题号区间。
- **缺陷已修（用户选择「修徽标 + 加排序说明」）**：
  1. 年份徽标统一降序：改写 outputs/政治-5周冲刺保底方案.html 中 8 处 `<span class="yr">`（**注意：`.yr` 徽标只存在于离线报告，DB/builtin-must-read.ts 里没有该字段，页面不渲染徽标**，故无需重灌库）；英语/数学两份报告复检均为 0 处异常。备份 /tmp/pol_sprint_backup.html。
  2. 排序说明：在第四章导语末尾追加「<b>排序说明：</b>章节按投入产出比排列（毛中特 → 习概 → 哲学），不是卷面题号顺序…」——**双处同步**：outputs/政治-5周冲刺保底方案.html 的 `<p class="sub">` + builtin-must-read.ts 的 chapterIntro（38 条，replace_all），再 `pnpm db:must-read`（写入 560 条，自检通过）。
- 验收：API `GET /api/v1/knowledge?subject_id=1&origin=sprint5w&order=sort` 返回 38 条含「排序说明」；Playwright 实测 PC `.mr-chapter-intro` ×3、H5 `.mmr-intro` ×1（H5 章节默认折叠，须先点 `.van-cell` 展开）。截图 outputs/排序说明-PC政治必背.png、排序说明-H5政治必背.png、年份徽标修复-离线报告.png。
- ⚠️ **Playwright 登录态坑（新增）**：① 只做 route 拦截不够——store `restore()` 拿不到 localStorage token 会直接跳 /login，必须 `add_init_script` 写 `shck_token` **且**拦截 `/api/v1/**`；② vite 的 proxy 默认指向 `8.218.204.8:8080`（生产容器），其 JWT secret 与仓库根 .env 不同 → 自签 token 必 401；必须在 handler 里把 URL 改写到 `http://127.0.0.1:3000/api/v1` 才通（本机 API 用根 .env 的 secret）。

## 政治「背旧知识考新卷」留出法实测（22:50，用户要求严格实测）
- 问题：只学 2024 及以前的知识去考 2025 卷能得多少分。方法：训练集＝2014-(Y-1) 全部政治真题，测试集＝Y 卷；Y 取 2023/2024/2025 三年交叉验证。
- 口径（关键）：
  1. **原题答案级（最严）**：正确项文本与往年正确项完全一致 → 2025 仅 3/35。
  2. **自动「答案关键子串」口径（主口径）**：答案的 4-6 字连续子串（剔除社会主义/中国特色等泛词）在往年题干或选项中出现过即算"见过" → 逐年稳定：2025 非时政 20/30、2024 18/30、2023 20/30。
  3. **主观题**：答案与旧库的 4-6 字子串重合率 → 踩点给分模型估 2025 33.4/80、2024 37.3/80、2023 32.4/80。
- **结果（三年高度一致）**：选择题 ≈ 45-48/70，主观题 ≈ 32-37/80，**总分 ≈ 80/150（约 53%）**；严格口径下探到 63/150。三年分别是 80 / 82 / 81 分。
- 明细发现：2025 时政 5 题（2024.7–2025.2 事件）在旧库零覆盖；2025#41（自我革命/历史周期率）、#38（新民主主义革命与社会主义革命的关系）等为全新表述；习概「两个结合/伟大建党精神/全过程人民民主」等新表述集中在 2024-2025 才出现。
- ⚠️ 数据口径备注：必背卡的 `refs` 只登记 2021-2025（少量 2014-2020），**不能据此断言考点从未考过**——判断"往年是否考过"必须直接 grep `exam_questions.content + options_json`（本次即如此）。另：2025 卷为网友回忆版；2023 卷录入顺序错乱（按题号分板块会偏，但不影响本结论）。
- 脚本：/tmp/holdout.py（M0-M3 口径）、/tmp/holdout2.py（子串/同义口径）。

## 产出：政治选择题逐年命中实测 HTML（23:00）
- 文件：`outputs/政治选择题-逐年命中实测.html`（529KB，455 道选择题全量，2014-2025 共 12 套）。生成脚本 /tmp/gen_hit_report.py（判定）+ /tmp/build_hit_html2.py（出页面）。
- **三级判定口径（重要，勿混）**：① 原文级＝正确项原文此前原样出现过；② 强命中＝命中 ≥5 字片段，或 ≥2 个不同 4 字片段；③ 弱命中＝只命中 1 个 4 字片段（可能是巧合片段）；④ 未命中。
- 总数：原文级 50 / 强命中 138 / 弱命中 58 / 未命中 209。逐年强命中率：2015 18%→2018 52%→2020 57%→2021 63%→2024 40%→2025 49%。
- 2025 卷：强命中 17 / 弱命中 3 / 未命中 15（含时政 5 题全未命中）。
- ⚠️ **口径陷阱**：早期用「宽松口径」（1 个 4 字片段即算命中）得 2025 = 20/35，但 4 字片段会误配（如「观世界的」），按严格口径 2025 降为 17、2024 从 18 降为 9。**做命中率统计必须用「≥5 字 或 ≥2 个 4 字」，否则高估 ~30%**（全库 241→168）。
- ⚠️ 建倒排索引时若只索引往年「答案」文本，会漏掉题干/选项里的考点（2025 会从 20 掉到 12）——必须索引题干+选项全文。
- ⚠️ CSS 类名污染：状态类（.lv-miss 等）若同时用于 tag 和 article，整卡会染上背景色 → 状态类要用 `.tag.lv-*` 限定，卡片侧栏用 `border-left-color` 上色。
- 页面交互：年份 `<details>` 折叠（2024/2025 默认展开）、关键词搜索、只看未命中（209 条）、展开/收起全部；Playwright 复验 0 JS 错误。截图 outputs/命中实测-逐题细节.png、命中实测-总览.png。

## 「未命中题怎么做取舍」实测结论（纯分析，未改任何文件）
- **板块未命中率（2021-2025, n=175）**：习概 40%（20/50）＞时政 56%（14/25，题量小）＞哲学 28%（14/50）＞毛中特 24%（12/50）。**每年期望损失**：习概 4.0 题 / 时政 2.8 / 哲学 2.8 / 毛中特 2.4，合计 **12 题≈24 分/年**。
- **可回收性分层（方案）**：习概（背 2024-25 新表述，现在起持续，回收 ~2.5 题）＝时政（考前 10-14 天背近一年时政，回收 ~2.5 题）＞哲学（理解+排除，做题非背，回收 ~1.0）＞毛中特（不加码，~0.8）。合计 ≈6.8 题 ≈ **+13.6 分**，客观题命中率 48%→68%。
- **时政窗口实测**：2024 卷考 2023.9-2024.6；2025 卷考 2024.7-2025.2 → **「考前约 12 个月的重大时政」，偏重最近半年**。2026-10-17 考试 → 重点窗口 2025.10-2026.9。
- **蒙题技巧回测（209 未命中题，随机基线 25%）**：✅唯一最长 **31.1%**（74 题可判定；2021-25 子集 33.3%、全库 35.6%）；剔除绝对化词后唯一最长 31.6%；含正面词唯一最长 30.3%；⚠️含并列连词最多 41.7%（**仅 24 题，样本太小不可用**）；政治正确词最多 27.8%（全覆盖，微弱）；题干关键词重合最多 24.4%（≈随机）。
- **⚠️ 被否掉的「常识技巧」（勿用）**：① 绝对化词——正确项含 2.4% vs 干扰项 2.0%（1.22×，无差异）② 字母分布 A116/B116/C112/D111 完全均匀 ③ 最相似选项对含正确项 53.4%（随机 50%）④ 「答案总是最长」——答案∈最长集合 66.4%，但**随机基线本就 60.7%**（选项普遍并列同长），仅 +5.7pp，且未命中 2021-25 子集 **-1.2pp 无增益**。早期把「最长法」说成 66.6% 是把「分布偏长」误当「可操作增益」。
- ⚠️ 2023 卷题号位置不可用于判板块（录入顺序与真卷有出入，31-35 号位实为哲学题）。

## 交付：`outputs/政治-未命中题取舍方案.html`（22.9 KB，纯静态无外部资源）
- 定位＝《政治-5周冲刺保底方案》的**配套方案**，只解「客观题 70 分命中率 48%→68%」，不碰主观题。复用 5 周报告的 `--acc/--red/--grn/--amb` 令牌与 `.hero/.metric/.box/.pill` 类，新增 `.bkt`（四桶卡）/`.tl`（时间轴）/`.rule`（考场规则步）。
- 八节：诊断表 → 四桶取舍 → 考场 4 条规则 → 被否技巧表 → 25 天日程（9/22→10/17 三阶段+考试）→ 收益表 → 不要做四件事 → 口径局限。
- 关键：**距考试 25 天**（今天 9/22，考 10/17）。阶段一 9/22-10/2 习概增量区+哲学刷题；阶段二 10/3-10/10 时政启动（10/3＝考前 14 天）；阶段三 10/11-10/16 时政收口+考场规则演练。
- ⚠️ 不可编造 2025.10-2026.9 的具体时政事件/新表述（超出知识范围）→ 方案只给**窗口规则 + 来源类型 + 摘录模板**，具体内容由用户按官方原文填。这是刻意的，不是遗漏。
- 验收：Playwright 全页 1180px 高 4652、无横向溢出、0 控制台错误；360px 无溢出元素；四桶头配色 grn/acc/amb/#8a8a8a 均实测命中。截图 `取舍方案-全页.png`、`取舍方案-移动端.png`。

## 交付：PC 取舍台账模块 `/tradeoff`（新增路由 + 科目维度，暂时只有政治）
- **后端**：`entities/tradeoff-item.entity.ts`、`tradeoff/{module,controller,service,dto}.ts`、迁移 `2026092201-tradeoff-ledger.ts`、`builtin-tradeoff.ts`(15 条首灌) + `import-tradeoff.ts`（幂等，只删 `origin='tradeoff-seed'`）。app.module 与 data-source 双侧注册。根脚本 `pnpm db:tradeoff`。
- **前端**：`api/tradeoff.ts`、`views/TradeoffView.vue`、`utils/tradeoffPlan.ts`（四桶元数据/考场规则/被否技巧/25天日程）；路由 `/tradeoff` + 侧栏导航「取舍台账」（漏斗图标）+ 面包屑。
- **首灌内容**：习概 7 领域采集位(pending) + 时政 4 类采集位(pending) + 哲学 3 张训练任务(done) + 毛中特 1 张状态卡(done) = 15 条 × 2 账号。习概/时政只给「收什么/从哪收」的采集位，**不编造 2025.10-2026.9 的具体时政与新表述**。
- ⚠️ 踩坑 ①：`BucketStat` 接口未 export → TS4053「cannot be named」，导出即解。
- ⚠️ 踩坑 ②：`MobileMustReadView.vue` 引用 `extra.table.rowMeta` 而 `KnowledgeTable` 无此字段 → **既有类型错误，阻塞 `pnpm build`**。库里数据确有 `rowMeta:[{tag,level}]`，已补类型字段并把 MustReadView 的 `table: any` 收紧为 `KnowledgeTable`。
- ⚠️ 踩坑 ③：种子正文写了 Markdown `**粗体**`，页面按 `pre-wrap` 纯文本渲染 → 星号原样显示。已全部改为「」。
- ⚠️ 踩坑 ④：`patchQuery` 用 `router.replace`，**没有历史记录**，Playwright 测「清筛选」必须点「全部桶」而不是 `go_back()`。另：本会话 `&` 后台进程随 Bash 调用结束被杀，起长驻服务要用 `run_in_background`。
- 验收：`/tmp/verify_tradeoff.py` 全通过 —— 接口 200/total 15、四桶 7/4/3/1、状态 11 pending + 4 done、四桶卡 4/条目 15/日期组 1/顶部指标 5、桶筛选 4 条、状态流转 pending→done、星标切换、方案说明 4 规则+5 否掉技巧+4 日程、表单 7 字段、1440 宽无横向溢出、0 控制台错误。截图 `取舍台账-PC.png`、`取舍台账-方案说明.png`、`取舍台账-补录表单.png`。

## 考点增补模块返工：命名 / border-left / 正文口径（用户三连指正，23:00-24:00）
- 用户原话：「取舍台账你这起的什么意思 还有card不让加border-left颜色你怎么又加 还有里面的录入内容是让人看的吗 考虑用户角度了嘛」。经 AskUserQuestion 确认：页面名＝**考点增补**、卡片桶标识＝**卡内小色点 + 名称着色**、内容定位＝**要背的内容**。
- **① 命名**：「取舍台账」是内部工作语 → 用户可见文案全改「考点增补」（h1 / eyebrow「必背清单之外 · 政治」/ 侧栏 nav + 图标换成加号圆环 / 面包屑 / 空态 / 表单标题「增补一条」/ 列表头「增补记录」）。**路由 `tradeoff`、表 `tradeoff_items`、接口 `/api/v1/tradeoff`、文件名一律保留**，避免动迁移与 API。
- **② 去 border-left**：`.to-bucket` / `.to-entry` 的 `border-left: 3px solid var(--tone)` 删除（该写法只允许行内内容块，页内仅 `.to-stage` 时间线保留）。改为 `.to-bucket-name::before`(9px 圆点) + `.to-entry-bucket::before`(6px 圆点)，名称取 `var(--tone)`。`TRADEOFF_PLAN` 的 tone 由裸 hex 换成令牌：`--wb-warn` / `--wb-brand` / `--wb-accent` / `--wb-faint`（原本就是这些值，改后零色差）。
- **③ 正文重写**：15 条全部从「采集位（收什么/从哪收/为什么）」改写为**可直接背诵的原文 + 关键要素 + 出处**；状态由 11 pending + 4 done 统一为 15 done。方法论移入新常量 `ENTRY_RULES`，落在页面「方案与口径」面板。内容覆盖：2025-12 中央经济工作会议总基调与「十五五」三项硬指标（研发投入年均 +7%、数字经济核心产业占 GDP 12.5%、单位 GDP 碳排放累计降 17%）、二十届四中全会与「十五五」开局、抗战胜利 80 周年与年度纪念节点、2026 中央一号文件（第 14 份）、生态环境法典（2026-03-12 公布 / 08-15 施行，第二部法典）、全面从严治党与中央纪委五次全会、**全球治理倡议**（2025-09-01 上合天津峰会，第四个全球性重大倡议）；时政四类补金上—湖北 ±800kV 特高压、神舟二十一号、菜园坝长江隧道「穿江不减速」、2025 GDP 破 140 万亿、2026 七一勋章 8 人、航天功勋奖章等。
- **④ 顺带订正事实错误**：时政窗口原写「2025.10–2026.9（考前 12 个月滚动）」，与考纲冲突。考纲原文＝「上一年 7 月 1 日至考试当年 6 月 30 日」→ 本年度应为 **2025-07-01 → 2026-06-30**；旧口径既漏 2025 年 7—9 月（上合天津峰会、抗战 80 周年），又把未发生的 7—9 月当考点。同步改 `tradeoffPlan.ts`（新增 `SZ_WINDOW`）+ `SCHEDULE` 文案 + 离线报告 `outputs/政治-未命中题取舍方案.html`（备份 /tmp/pol_tradeoff_report_backup.html）。窗口外事件（2026 年 7 月菲尔兹奖、9 月世界技能大赛）明确标注不计入。
- ⚠️ **工具坑复现**：同一条消息里对**同一文件**并行发多个 Edit，工具全部回报 Successfully edited，但实际只有部分落盘（本次 tradeoffPlan 5 处 tone 改动 + TradeoffView 6 处文案全丢）。**改为逐文件串行 / 整文件 Write**，改完用 Python `str.count()` 复核，不要只信 Edit 返回信息。
- 验收：`vue-tsc --noEmit` 0 错；`pnpm --filter @shck/server build` 通过；`vite build --emptyOutDir false` 通过；`pnpm db:tradeoff` 15 条 × 2 账号 + 自检通过；Playwright `/tmp/verify_tradeoff.py` **31 项断言全过**（h1/侧栏＝考点增补、页面无「取舍台账」、四桶与条目 `borderLeftWidth` 均 1px、桶名 ::before 9px、条目桶 ::before 6px、15 条、正文含上合天津峰会且不含「采集窗口」、时政筛选 4 条、星标可切、方案面板含补录口径与 2025-07-01 → 2026-06-30、1440 无溢出、0 控制台错误）。截图 outputs/考点增补-PC页面-上半屏.png、-条目区.png。
- 技能 `pol-tradeoff-daily-entry` 同步重写：改名、补三条红线（正文口径 / 不得 border-left / 窗口以考纲为准）、补「同文件并行 Edit 会互相覆盖」章节。
