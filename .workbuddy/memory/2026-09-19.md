# 2026-09-19

- 解答「必背考点为何不按题序排」：排序 = `sort_order`（章节→分组→组内），章节结构来自 5 周冲刺方案；必背卡是人工归纳高频卡，`extra` 契约无题号字段，故无法按题序排（详见 MEMORY.md §10）。
- 用户确认 **outputs/ HTML 可能落后，权威源已转移到 PC 端必背考点模块（DB），真题库同步为准** → 已写入 MEMORY.md §3/§10。
- 清理 MEMORY.md：合并冗长段落（§7/§8/§10 压缩），修正注入截断问题，保留全部关键事实。
- **命中标注落地**：调查后发现 builtin 文件的 refs 是人工整理的完整证据卡（政治 102 卡 162 道 / 数学 45 卡 116 道），前端 MustReadRefs.vue 早已实现渲染+跳真题页；真正的问题是 **DB 灌库后文件又迭代过，库里缺 refs/priority**。2026-09-19 跑 `pnpm db:must-read` 幂等重灌 560 条同步，playwright 验收 8/8 + 5/5（含 KaTeX、跳 `/papers/:id?no=`）。期间一度想用关键词匹配自动生成 refs 回写——**危险，已放弃并删除脚本**（会破坏人工证据卡）。
- 踩坑记录：mysql2 JSON 列返回对象导致探针误判覆盖率 0；多行 JSON grep 漏配；pnpm 需 corepack shim。

## 2020 高数一真题核对（23:55）
- 用户提供 6 张真题册实拍（全卷 1–28 题完整），核对库内 paper_id=43「2020 高数一（网友回忆版）」。
- 结论：系统性差异，非零星笔误。P0×4：缺解答26（y=x³−3x²+2x+1 凹凸拐点）、多真题没有的旋转体体积题（库Q15）、选择9题干不同（库 x→0 vs 真题 x→1，答案 1/2 vs 3/2）、填15点 (0,2) 应 (0,π/4)；另解答分值 6/8… 应 8×5+10×3、库Q11 answer 双【答案】块脏数据。P1：9/10 道选择题选项排列与真题不同→字母错位（值都对）；全卷题序≠官方题号。连带：builtin-must-read.ts 有 1 张「2020 · 第 9 题」证据卡用的回忆版题干。
- 报告：outputs/高等数学一-2020真题核对.html（方案A全量重录/方案B最小修补）。**主库未动，等用户拍板。**
- MEMORY.md 已按上限压缩重写（新增 2020 卷差异条目）。

## 2020 高数一：按真题全量重录（用户拍板方案 A，00:10 执行）
- 脚本 `/tmp/gs2020_reload.py`：DELETE paper_id=43 全部题后按官方题序重插 28 题（选择1-10/填空11-20/解答21-28，4×10+4×10+8×5+10×3=150）。答案字母 DC A A B B D A C B；公式用 `\mathrm{d}x`；answer 格式 `【答案】…\n【解析】…`（客观题解析末带"故选 X"）；填空/解答 options_json=NULL。
- exam_papers：title→「2020 年高等数学（一）真题」，source→真题册实拍，source_type→**VERIFIED_OFFICIAL**（其余年份仍为 VERIFIED_RECALL），verification_notes 写满修订 6 条。
- 必背考点证据卡同步：DB 里引用 2020 的共 7 张卡×2 用户=14 条，题号按官方重映射（隐函数21→20、换元13→24、分部积分14→21、面积22→19、微分方程16→25、极限9 题干改 x→1 版 C=3/2）；旋转体体积卡的 2020 引用**摘除**（真题无此题）。**builtin-must-read.ts 必须同步改**（脚本 `/tmp/gs2020_ts_patch.py`，改完 `tsc --noEmit` 校验通过），否则下次 `pnpm db:must-read` 会覆盖回旧值。
- 冒烟：起 `packages/server/dist/main.js` + JWT（自签 HMAC，/tmp/gs2020_smoke.py）`GET /api/v1/papers/43` → 28 题/结构正确。⚠️ **起服务与 curl 必须在同一条 Bash 里**（跨调用 localhost 不通：先 502 后 Connection refused）；需 `NO_PROXY=127.0.0.1,localhost`。
- 遗留：localStorage `shck_practice_paper_43` 题序已变，建议清空该卷进度。

## 英语 2024 阅读「定位法 4 步」实战审计（00:40）
- 方法卡：knowledge_items id=4250（MATERIAL，英语《四、阅读理解 60 分：定位法 4 步》，subject_id=2/8）。2024 英语真题 paper_id=56，阅读=sort_order 35–54（真题 36–55，20×3=60 分，psg 在 `passage` 列）。
- 方法：**盲测**（导出不含答案的卷 /tmp/eng2024_blind.txt，答案另存 /tmp/eng2024_key.json），派 subagent 分档模拟，prompt 里明令禁止读 DB/项目文件/联网。
- 结果：词汇 2000→60/60（高置信13）；1200→60/60（高11）；100→60/60 但仅 2 题高置信（运气）；50→**42/60**（高7中7低6，**低置信 6 题全错**）。零词汇档期望≈48（区间 42–60），**稳拿 42 分 = 14 题纯字形匹配**。
- 核心发现：零词汇者最大敌人不是生词而是「原文原词陷阱」——Q39 process photos、Q42 90% less water vs more、Q48 cooperation+fair play 拼成 balance，3 次全中招。方法卡第4步只写了绝对词（本卷仅用 1 次），缺「原词拼凑」「比较级反向」两条。方法卡也未覆盖推断/态度/出处 3 类（9 分）。
- 产出：outputs/英语-2024阅读定位法审计.html（四档对比+逐题审计+改进建议+局限）。
