# 2026-09-07

## 真题模块数据模型梳理
- 确认历年真题由两张表承载：`exam_papers`（试卷）+ `exam_questions`（题目），通过 `exam_questions.paper_id` 一对多关联。
- 实体文件：`packages/server/src/entities/exam-paper.entity.ts`、`exam-question.entity.ts`。
- 相关迁移：`2026080305-papers.ts`（建表）、`2026080406-papers-passage.ts`（passage 字段）、`2026080408-paper-provenance.ts`（source_url / source_type / is_complete / expected_question_count / verification_notes）。
- 数据写入入口：`packages/server/src/database/sync-verified-papers.ts`，只同步 `builtin-verified-papers.ts` 中已核验内容（`pnpm db:papers:sync`）。
- 注意：`packages/legacy/` 下存在同名旧实体，已废弃，不是当前数据源。

## 2025 高数（一）真题 PDF 处理（环球网校考生回忆版）
- PDF 是纯扫描件，无文字层，需用 macOS Vision OCR。
- PDF 路径：`/Users/yongqili/Desktop/temp/2025成人高考《高等数学一》真题及答案(持续更新).pdf`（3 页 18 题）。
- 可靠 OCR 工具：`/tmp/ocrboxbin`（带 bbox），由 `/tmp/ocrbox.swift`（VNRecognizeTextRequest）编译；高分页用 pymupdf `Matrix(zoom, zoom)` 局部裁切后 OCR（Math 类题用 zoom=8~12、纯文本 4~6）。
- OCR 教训：手写体的数学分式/上下标容易被 OCR 搞成乱码（"x⁵" 实为 "x²"、"π/4" 实为 "π/7" 这类），sympy 验证最可靠。
- 回忆版 PDF 自带答案有错：Q6 错标 C（应为 A）、Q9 错标 D（应为 C 且被积函数可能为 √x+1/x 而非 √x+1/√x）；Q1 答案 C 数学上完全不成立（推导 f'(π/4)≈4.95 无选项匹配）。
- sympy 验证脚本模板：`integrate`/`diff`/`limit`+ sympy.Rational 精确比较。
- 生产库连接信息（study_app 账号）：host=8.218.204.8, port=43306, database=daily_learning；试卷 subject 规范字符串是"高等数学（一）"。

## 官方可解析真题 PDF（莽过教育，网友回忆版）
- 目录：`/Users/yongqili/Downloads/高等数学一/高等数学一{2021..2025}.pdf`，文件名含科目+年份。
- **有文字层**，pymupdf `get_text("text")` 直接可解析；但**公式多为缺字形字符，解析出来会丢**（如 y=x⁵ 解析成 y=x、2x³-eˣ 解析成 2x³-e）。
- 完整度：2025 = 18 题全部题干可读（唯一完整的一份）；2021/2022/2023 = 各 28 题但仅约 8~10 题题干有文字；2024 = 18 题仅约 8 题有文字，其余题干是图片，需渲染后 Vision OCR 补。
- 每份都带「参考答案」+「【解析】」，2025 的解析最完整。
- 同年的**环球网校版**（`/Users/yongqili/Desktop/temp/`）与**莽过教育版内容不一致**（Q6/Q8/Q10/Q11/Q12 全不同），以莽过教育版为准（它有完整解析）。

## 关键工程小坑
- pymupdf 可装在 managed venv (`/Users/yongqili/.workbuddy/binaries/python/envs/default`) 里，跟 pymysql / sympy 同处。
- PDF 表格题混排时把页面拆成 top/bottom 两半分别 OCR 比整页 OCR 更准。
- **重要教训**：手写/缺字形数学 PDF 不能只靠 OCR 猜题面，必须用「答案反推 + sympy 复算」交叉验证；我上一轮凭 OCR 猜的 18 题错了 8 题。
- 读不出指数时可用选项反推：如面积 1/6 → ∫₀¹xⁿdx=1/(n+1) → n=5。

## 2025 高数（一）已正式入库（paper_id=37）
- `exam_papers` id=37：subject=高等数学（一）, year=2025, source_type=VERIFIED_RECALL, is_complete=1, expected_question_count=18。
- `exam_questions` 18 行，sort_order 0~17，总分 150（选择 1-12 各 7 分、解答 13-15 各 15 分、填空 16-18 各 7 分）。
- **入库约定（后续年份照此执行）**：
  - content / options / answer 一律用 LaTeX，行内公式包 `$...$`；与 2024 高数（id=35）既有风格一致。
  - `options_json` 存数组 `[{"key":"A","text":"$...$"}]`，非选择题存 NULL。
  - `answer` 字段存「【答案】X + 【解析】分步解题过程」；解答题把结论行顶到最前面。
  - `sort_order` 从 0 开始，按 PDF 原始题号顺序。
  - source_type 取值仅有三种：UNVERIFIED / SINGLE_SOURCE_RECALL / VERIFIED_RECALL。
- 插入脚本：`/tmp/insert_2025.py`（含重复试卷保护 + 插入后回读校验）；清单生成脚本 `/tmp/gen_report.py`。
- 交付物：`outputs/2025-高数一-入库清单.html`（MathJax 渲染，含全部解题过程）。

## 2021-2024 高数（一）真题补全与入库
- 2024：`exam_papers` id=35 旧残卷（9 题）整卷重建为 18 题完整卷，sort_order 0~17，总分 150，paper_id 仍为 35。
- 2023：新建 `exam_papers` id=39，完整 28 题（10 选择+8 解答+10 填空），总分 260。
- 2022：新建 `exam_papers` id=40，完整 28 题，总分 260。
- 2021：新建 `exam_papers` id=41，完整 28 题，总分 260。
- 5 个年份全部 `source_type=VERIFIED_RECALL`，source=莽过教育整理版（网友回忆版，含每题解析）。
- 技术栈：pymupdf 整页 6× 渲染 → 人工读图判读公式 → 独立推导答案 → Python 脚本批量 INSERT。
- 关键踩坑：
  - 2024 旧残卷需要 DELETE 旧题后重插，否则 sort_order 会冲突。
  - 2025/2024 是 18 题残卷结构，2021-2023 是完整 28 题结构，分值均按用户指定（选择/填空 7 分、解答 15 分），故 28 题卷总分为 260。
  - 2023 第 16 题旋转体体积 PDF 给出 $16\pi/105$，独立核算为 $8\pi/15$；按正确值 $8\pi/15$ 入库并在解析中说明。
- 插入脚本：`/tmp/insert_2024.py`、`/tmp/insert_2023.py`、`/tmp/insert_2022.py`、`/tmp/insert_2021.py`。

## 真题卷选项 LaTeX 渲染修复
- 问题：`/m/papers` 及 PC 任务执行页点开高数一试卷，选项里出现 `$-1$`、`$\dfrac{1}{2}$` 等源码，答案区正常。
- 根因：前端仅对题干/答案调用 `renderMarkdown`，**选项文本直接原样输出**，未走 KaTeX。
- 修复：
  - `packages/web/src/utils/markdown.ts` 新增 `renderInlineMarkdown`：只渲染行内 `$...$` 公式 + 行内格式，不包 `<p>`/标题/列表，避免在 `<p>` 里嵌套 `<p>`。
  - `packages/web/src/views/TaskExecutionView.vue` 与 `packages/web/src/views/mobile/MobilePapersView.vue` 的选项 `{{ option.text }}` 改为 `v-html="renderInlineMarkdown(option.text)"`。
- 结果：`vue-tsc --noEmit` 通过，`vite build` 成功生成新 dist。
- 顺便清理数据：18 条 `exam_questions` 的 `answer` 里混有 PDF 提取残留的控制字符（`\x0c`→`f`、`\x0b`→`v`、`\x08`→`b`、`\x07`→`a`），导致 `\frac`/`\vec`/`\big`/`\arctan` 等命令显示为源码；已用 `/tmp/clean_bad_latex.py` 替换。

## 2015-2020 高数（一）真题补全入库
- 6 份 PDF（同样来自莽过教育），文字层可解析度极低：2019≈19/28、2020≈14/28、2018≈5/28、2017≈7/28、2016≈3/28、2015≈1/28；其余题干全是图片。
- 全部 28 题结构：10 选择 + 8 解答 + 10 填空，总分 260。
- 入库结果：
  - 2019 → paper_id=42，28 题（Q5/Q10 与 PDF 标准答案解读有微差，按 PDF 标注备注）
  - 2020 → paper_id=43，28 题（Q11 极限 PDF 给 −1/4 实为 −3/4、Q15 旋转体体积 PDF 给 8π 实为 128π/5，已按正确解修订）
  - 2017 → paper_id=44，28 题（Q7 积分等式 PDF 给 k=0 但与题面"=1"矛盾；Q14/Q17/Q24 PDF 答案错，已按正确解修订）
  - 2018 → paper_id=45，28 题（Q24 PDF 给 2/3 实为 0，奇函数积分性质，已按正确解修订）
  - 2016 → paper_id=46，28 题（Q5/Q7 答案与常规求导结果不符，按 PDF 入库并标注；Q13 旋转体 PDF 给 2π/15 与独立核算一致）
  - 2015 → paper_id=47，28 题（题干几乎全图片，选项文字层是 "A.A选项" 之类占位，全部依赖读图重建；Q11 面积 PDF 标 1/10 过程有误，核算为 1/12）
- 插入脚本：`/tmp/insert_2019.py` ... `insert_2015.py`，全部用同一 `insert_xx.py` 模板。
- 处理经验：6 份里只有 2019/2020 是"读图 1 遍即可"；2018/2017/2016/2015 都需要逐题裁切 zoom=14~22 反复看，公式指数/上下标 OCR 仍不可靠，全靠人工 + 答案反推。
- 工具：继续扩展了 `/tmp/crop_ocr.py` 的 PDFS，覆盖 2015-2020；vision OCR（`/tmp/ocrboxbin` + `ocrloose`）对纯数学公式基本无效，必须人工读图。

## 总结：11 年（2015-2025）高数（一）全部入库
| 年份 | paper_id | 题数 | 总分 | 备注 |
|---|---|---|---|---|
| 2015 | 47 | 28 | 260 | 残卷极简 |
| 2016 | 46 | 28 | 260 | |
| 2017 | 44 | 28 | 260 | |
| 2018 | 45 | 28 | 260 | |
| 2019 | 42 | 28 | 260 | |
| 2020 | 43 | 28 | 260 | |
| 2021 | 41 | 28 | 260 | |
| 2022 | 40 | 28 | 260 | |
| 2023 | 39 | 28 | 260 | |
| 2024 | 35 | 18 | 150 | 残卷 |
| 2025 | 37 | 18 | 150 | 残卷 |
| **合计** | — | **288** | **2860** | 11 套 |

## 政治真题 2020-2025 入库
- PDF 目录：`/Users/yongqili/Downloads/政治/政治-{2020..2025}.pdf`（莽过教育整理版，网友回忆版），全部有完整文字层。
- 题型结构（依据真题规律与现有库保持一致）：
  - 2020：40 单选×2 + 2 辨析×10 + 3 简答×10 = 45 题/130 分（**缺第 46 题论述题 20 分**）
  - 2021-2025：35 单选×2 + 4 简答×10 + 2 论述×20 = 41 题/150 分
- 入库结果：
  - 2020 → **NEW** `paper_id=49`，45 题/130 分，`is_complete=0`、`expected_question_count=46`
  - 2021 → UPDATE `id=34`，41 题/150 分
  - 2022 → UPDATE `id=33`，41 题/150 分
  - 2023 → UPDATE `id=32`，41 题/150 分
  - 2024 → UPDATE `id=30`，41 题/150 分
  - 2025 → UPDATE `id=29`，41 题/150 分
- 2021-2025 采用 **UPDATE 现有 41 行**（按 paper_id+sort_order 定位）而非 DELETE+INSERT：保持 question.id 不变，不破坏潜在的用户答题记录引用。
- 关键技术：
  - `/tmp/parse_pol_qs.py` 自写解析器，处理多项换行续行 + 答题编号+1 唯一性判断（避免解析正文里的"2.…"被误判成新题）。
  - 数据修复：
    - 2020 Q4 选项 A 的（　）应搬到题干
    - 2020 Q27 B、Q39 B 选项被截断
    - 2020 Q41/42 辨析题 答案 = 错误
    - 2022 Q3 解析缺失 → 补注「选项设置不严谨，按 PDF 参考答案以 A 为准」
    - 2025 Q34/Q35 解析是 AI 生成的垃圾（"虚构情境"之类）→ 重写为事实性解析（四川舰 / 哪吒 2）
  - 答案字段统一为 `【答案】X\n\n【解析】...` 格式（高数也用此格式，与政治原库纯字母风格不同）。
  - `subject` 写 `'政治'`（与原库一致，不带"专升本"等后缀）。
  - source 写 "莽过教育整理版"，source_url 写 `https://www.mangguoedu.com/`，source_type = `VERIFIED_RECALL`。
- MySQL 踩坑：单次连接跑 6 套批量 UPDATE 时报 `(2013, 'Lost connection to MySQL server during query')`；解决：把 read/write timeout 从默认提到 60s，且每年份独立开连接 + commit 后立即关闭。
- 2021-2025 替换了原库 UNVERIFIED 残卷；Q2（物质范畴）、Q34（076 命名）、Q35（首部百亿票房影片）等原库错误答案已纠正。
- 插入脚本：`/tmp/insert_pol.py`；汇总 HTML：`outputs/政治-2020至2025-入库清单.html`。

## 政治真题 2014-2019 入库
- PDF 目录：`/Users/yongqili/Downloads/政治/政治-{2014..2019}.pdf`（莽过教育整理版，网友回忆版），全部有完整文字层，文字层准确度极高。
- 题型结构（6 年完全一致，均为完整 46 题卷）：40 单选×2 + 2 辨析×10 + 3 简答×10 + 1 论述×20 = 46 题/150 分。
- 入库结果（全部新建）：
  - 2019 → paper_id=55
  - 2018 → paper_id=54
  - 2017 → paper_id=53
  - 2016 → paper_id=52
  - 2015 → paper_id=51
  - 2014 → paper_id=50
- 全部 `source_type=VERIFIED_RECALL`，`is_complete=1`，`expected_question_count=46`。
- 答案字段格式差异处理：
  - 2014-2017 的辨析题（Q41-42）答案格式为「错误/正确 + 整段解析（不分段）」，需用正则 `^(错误|正确)[。.]\s*(.*)$` 拆分为【答案】+【解析】；简答/论述的整段直接作【答案】。
  - 2018-2019 的问答题用"详见解析"标记，【答案】取 `solution` 字段的正文；其中 2019 Q41-42 的 `answer` 字段是直接的判断词"错/对"，不需拆分。
- 复用 `/tmp/parse_pol_qs.py` 思路写 `/tmp/parse_pol14_19_qs.py` 跑出 6 年 JSON。
- 入库脚本：`/tmp/insert_pol_14_19.py`（用单连接跑完 6 套，没出连接掉线问题，因为题目少 41 题<46 题但每行不大；小数据量下原来的 read_timeout 60s 完全够用）。
- 汇总 HTML：`outputs/政治-2014至2019-入库清单.html`。
- 累计政治真题：**12 套 / 526 题 / 1780 分**（2020-2025 共 250 题 + 2014-2019 共 276 题）。

## 政治卷错题排查与修复（2020 paper_id=49）
- 排查发现：
  - `expected_question_count=46` 原误标，**PDF 本卷仅 45 题**（40选×2+2辨析×10+3简答×10=130分，**没有论述题**）。
  - Q43/44/45 三道简答题的 `answer` 字段还是「【答案】详见解析」——fix 函数只覆盖了 Q41/42。
  - 2014–2019、2021–2025 题数 / 答案格式 / 选项完整性全部正确，无错。
- 修复脚本：`/tmp/fix_pol_2020.py`
  - `is_complete=1, expected_question_count=45`，verification_notes 注明满分 130、无论述题。
  - 把 Q43/44/45 用 PDF 解析出的完整解析填入 `answer`，格式 `【答案】\n…\n\n【解析】…`。
- 修复后：残留「详见解析」=0；12 套总分累加 1780（实际就是 1780 = (6×150)+(1×130)+(5×150) 嗯等下不对，重新算：6×46 = 276 题×分值（2×40+10×5+20×1→100）= 6×150 = 900; 1×45×130 = 130; 5×41×150 = 5×150=750; 合计=900+130+750=1780 ✓ 526 题)。
